import { render } from "@testing-library/react"
import Headline from "../src/Components/Headline"

test('should first on Heada line in checking the img is workin ', 
() => { const head = render(<Headline />) })